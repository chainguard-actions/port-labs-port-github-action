name: Test PATCH_RUN with link and externalRunId inputs (invalid credentials)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          baseUrl: 'https://api.getport.io'
          clientId: 'invalid-client-id'
          clientSecret: 'invalid-client-secret'
          operation: 'PATCH_RUN'
          runId: 'r_fake_run_id'
          link: 'https://github.com/my-org/my-repo/actions/runs/12345'
          externalRunId: 'external-run-abc123'
          status: 'SUCCESS'
          logMessage: 'Deployment completed successfully'
          summary: 'All steps passed'
      - name: Assert action failed with invalid credentials
        run: |
          echo "outcome: ${{ steps.action.outcome }}"
          if [ "${{ steps.action.outcome }}" != "failure" ]; then
            echo "FAIL: expected action to fail with invalid credentials"
            exit 1
          fi
          echo "PASS: action correctly failed with invalid credentials"
